enum PlayersNames {
  CROSS = 'cross',
  ZERO = 'zero',
}

const REMIS_WINNER = 'remis';

enum ZeroCrossIconSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export { PlayersNames, REMIS_WINNER, ZeroCrossIconSize };
